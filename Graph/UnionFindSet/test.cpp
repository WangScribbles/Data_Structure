#include <iostream>
using namespace std;

#include "UnionFindSet.h"

#if 0

int main()
{
	string a[] = { "张三", "李四", "王五", "赵六" };
	UnionFindSet<string> ufs(a, sizeof(a) / sizeof(a[0]));
	
	
	return 0;
}

#endif